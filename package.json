{
  "name": "grishka-ai-music-monorepo",
  "version": "1.0.0",
  "description": "Grishka Music - Global Music Streaming Service",
  "private": true,
  "scripts": {
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "build": "cd client && npm run build",
    "start": "NODE_ENV=production node server/src/index.js",
    "dev": "concurrently \"npm --prefix server run dev\" \"npm --prefix client start\"",
    "server": "npm --prefix server run dev",
    "client": "npm --prefix client start"
  },
  "keywords": [
    "music",
    "streaming",
    "playlist",
    "global"
  ],
  "author": "VMRcompany",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
